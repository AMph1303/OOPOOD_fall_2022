import java.lang.Math;
public class Rectangle {
    double length;
    double breadth;

    double area() {
        double rectArea;
        rectArea = length * breadth;
        return rectArea;
    }
}
